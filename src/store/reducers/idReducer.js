const initialState = {
    userID: null,
    roleID: null,
    activityID: null,
};

const idReducer = (state = initialState, action) => {
    switch(action.type) {
        default: 
            return state
    }
};

export default idReducer;